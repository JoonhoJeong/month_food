import React, { useEffect } from 'react';

function RecipeList(props) {
  let foodStr = props.match.params.foodStr;
  let month = props.match.params.month;

  return <div></div>;
}

export default RecipeList;
